# Frontend Tests

- **E2E (Playwright):** see [`e2e/README.md`](e2e/README.md) — projects, run commands,
  and one-time session capture.
- **Unit (Vitest):** colocated as `src/**/*.test.ts(x)`; run with `npm test`.
