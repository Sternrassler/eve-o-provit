# E2E Test Quick Start Guide

The E2E suite has moved to a Playwright projects setup (public/setup/auth) with a
one-time captured session. The canonical, up-to-date guide lives next to the tests:

→ [`frontend/tests/e2e/README.md`](../../frontend/tests/e2e/README.md)

It covers the project layout, run commands (`npx playwright test [--project=public]`),
and how to capture/refresh the session.
